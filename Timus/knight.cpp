#include <iostream>
using namespace::std;
int main()
{
	int n;
	cin>>n;
	string *pos = new string[n];
	for(int i=0; i<n;i++) 
		cin>>pos[i];
	for(int i=0; i<n; i++) {
		if(pos[i] == a1 || pos[i] == a8 || pos[i] == h1 || pos[i] == h8) {
			cout<<"2"<<endl;
		}
		else if(pos[i] == b1 || pos[i] == a2 || pos[i] == g1 || pos[i] == h2 || pos[i] 
	}
}
